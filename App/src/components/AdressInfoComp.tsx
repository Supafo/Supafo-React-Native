import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

interface IAdressInfoComp {}

const AdressInfoComp: React.FC<IAdressInfoComp> = () => {
  return (
    <View>
      <Text>AdressInfoComp</Text>
    </View>
  );
};

export default AdressInfoComp;

const styles = StyleSheet.create({});
